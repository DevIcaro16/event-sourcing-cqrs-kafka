services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: banking_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
